# PostgreSQL connection string (optional for the UX prototype)
DATABASE_URL=postgresql://postgres:password@localhost:5432/myapp

# Shared secret for scheduled notification jobs
CRON_SECRET=replace_with_a_long_random_string

# Optional product metadata
NEXT_PUBLIC_APP_TITLE=天使桥
NEXT_PUBLIC_APP_DESCRIPTION=连接人与人、人与资源
