# 天使桥工作区规则

## 黑客松开发约束

本工作区当前处于 72 小时黑客松限时开发阶段。所有设计、实现、评审和部署决策都必须优先服务于可运行、可演示的核心闭环，减少与当前 Demo 无关的防御性工程。

当前核心演示目标是：两名用户分别通过手机号验证码注册登录，自由填写档案并进入同一匹配池，由真实 AI 生成可解释匹配，双方确认、实时聊天、确认桥约开始与完成，并看到成长值变化。

- 当前是黑客松最后一天。以最快速度实现可演示功能为最高优先级，不为后续稳定性、扩展性或长期安全目标增加工作量。
- 优先完成端到端闭环，再增加语音、外围页面和长期架构；豆包匹配与小天整理属于当前核心闭环。
- 只实现当前演示能够到达的状态和错误处理，不为假设性的未来场景增加兼容层、复杂抽象或基础设施。
- 当前采用单后端实例、单 PostgreSQL、真实共享匹配池和 SSE 聊天；除非核心闭环明确需要，不引入 Redis、消息队列、微服务、多模型路由、WebSocket 或多实例协调。
- 当前使用阿里云短信验证码登录，并允许两个隔离的固定验证码展示账号；不实现邮箱、密码、OAuth、刷新令牌、找回密码和复杂 RBAC。
- 除最终编译和核心闭环测试外，减少重复审计、全量回归和非关键异常穷举；发现不影响现场主路径的问题时先记录，不阻塞功能实现。
- 不得省略会直接影响公开 Demo 的必要边界：密钥不得进入仓库、密码不得明文存储、用户只能访问自己的身份与会话、生产环境使用 HTTPS、核心流程与聊天状态必须在重启后恢复。
- 遇到取舍时，选择代码量更小、联调面更窄、现场演示更稳定的方案，并明确标注 Fixture、合成数据和暂未实现的能力。

## 前后端联调边界

- 前端最终设计稿、页面结构、组件、文案层级、导航、样式和交互表现由前端负责；后端联调不得擅自改动这些内容。
- 只有页面必须读取真实数据或触发核心业务动作时，才修改前端的数据绑定层，并优先局限在 API Client、状态连接和按钮事件绑定。
- 后端应适应已确认的前端产品设计，通过稳定 API 和 Product ViewModel 提供页面可直接展示的业务语义；不得把 CSS、布局、颜色、动画或组件结构放入后端。
- 联调前先逐页标记：静态展示、真实读取、真实写入、状态驱动。黑客松非核心内容允许保持静态，核心闭环不得以静态数据伪装完成。
- 必须后端驱动的核心闭环包括：认证与用户身份、个人资料、档案发布、AI 整理、匹配结果及理由、双方同意、实时聊天、桥约开始与完成确认、成长值和相应状态变化。
# 测试执行规则

- 本工作区在沙箱内运行 Vitest 会稳定触发 `spawn EPERM`，无法创建测试子进程。执行 `npx vitest run ...` 时直接使用已授权的沙箱外测试方式，不再先进行一次必然失败的沙箱内试跑；权限仅用于当前项目的 Vitest 测试。
