# 天使桥

天使桥是一个使用 Next.js 构建的独立资源互换社区应用。

这是独立运行的前端 UX 原型，包含首页、发现、桥接、消息、成长、我的与小天聊天等页面。数据通过 `src/lib/tsq` 的类型化 API 边界提供，当前使用本地 mock，后续可替换为真实后端实现。

## 开发

```bash
bun install
bun dev
```

项目可直接部署到 Vercel。
