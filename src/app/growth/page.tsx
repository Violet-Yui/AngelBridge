"use client";
import Link from "next/link";
import { ArrowLeft, Leaf } from "lucide-react";
import { ME } from "@/lib/tsq/data";
import { AppShell } from "@/components/tsq/app-shell";
export default function GrowthPage() { return <AppShell><header className="flex items-center gap-3 px-4 pb-3 pt-2"><Link href="/me" aria-label="返回我的" className="grid h-9 w-9 place-items-center rounded-full bg-white shadow-sm"><ArrowLeft className="h-4 w-4" /></Link><div><h1 className="text-[22px] font-bold">成长记录</h1><p className="text-xs text-muted-foreground">每一次连接都会让生命树更茂盛</p></div></header><div className="space-y-4 px-4"><section className="rounded-[22px] bg-[color:var(--soft)] p-5"><p className="text-sm text-[color:var(--deep)]">当前等级 Lv.{ME.level}</p><strong className="mt-1 block text-4xl text-[color:var(--deep)]">{ME.growth}</strong><div className="mt-3 h-2.5 overflow-hidden rounded-full bg-white"><div className="h-full w-[85%] rounded-full bg-[color:var(--primary)]" /></div><p className="mt-2 text-xs text-[color:var(--deep)]">距离下一级还差 220 成长值</p></section><section className="rounded-[22px] border border-[color:var(--border)] bg-white p-4"><h2 className="text-[17px] font-semibold">全部记录</h2><div className="mt-3 space-y-4">{ME.growthLog.map((g) => <div key={g.title} className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[color:var(--soft)] text-[color:var(--deep)]"><Leaf className="h-4 w-4" /></span><div className="flex-1"><p className="text-sm">{g.title}</p><p className="text-xs text-muted-foreground">{g.date}</p></div><b className="text-sm text-[color:var(--deep)]">+{g.delta}</b></div>)}</div></section></div></AppShell>; }
