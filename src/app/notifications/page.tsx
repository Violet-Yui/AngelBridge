"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import { ArrowLeft, Bell, Sparkles } from "lucide-react"; import { AppShell } from "@/components/tsq/app-shell"; import { tsqApi } from "@/lib/tsq/api"; import type { NotificationItem } from "@/lib/tsq/types";
export default function NotificationsPage(){const [items,setItems]=useState<NotificationItem[]>(); useEffect(()=>{tsqApi.getNotifications().then(setItems)},[]); return <AppShell><header className="flex items-center gap-3 px-4 pb-3 pt-2"><Link href="/" aria-label="返回首页" className="grid h-9 w-9 place-items-center rounded-full bg-white shadow-sm"><ArrowLeft className="h-4 w-4"/></Link><h1 className="text-[22px] font-bold">通知</h1></header>{!items?<div className="m-4 h-40 animate-pulse rounded-2xl bg-[#edf1eb]"/>:<div className="space-y-3 px-4">{items.map(item=><article key={item.id} className="flex gap-3 rounded-[20px] border border-[color:var(--border)] bg-white p-4"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[color:var(--soft)] text-[color:var(--deep)]">{item.kind==='bridge'?<Bell className="h-5 w-5"/>:<Sparkles className="h-5 w-5"/>}</span><div className="flex-1"><h2 className="text-[15px] font-semibold">{item.title}</h2><p className="mt-1 text-sm text-muted-foreground">{item.body}</p><p className="mt-2 text-xs text-muted-foreground">{item.time}</p></div></article>)}</div>}</AppShell>}
